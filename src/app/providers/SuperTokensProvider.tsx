"use client";

import { ReactNode } from "react";

// Temporary simple provider without SuperTokens
export function SuperTokensProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
